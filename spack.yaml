# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
# Instructions for editing this file are found in 
# https://github.com/ACCESS-NRI/model-deployment-template/blob/main/spack.yaml
# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
  - access-om3@git.2025.x.0
  packages:
    access3-exe:
      require:
        - '@git.share_prototype'
        - 'configurations=MOM6-CICE6'
    access-cice:
      require:
        - '@git.5-port_cmake_build'
        - 'io_type=PIO +openmp'
    access-mom6:
       require:
       - '@git.cmake_build +openmp'
    access-ww3:
      require:
      - '@git.1-cmake_build'
    access3-share:
      require:
        - '@git.share_prototype'
    esmf:
      require:
        - '@git.v8.7.0'
    fms:
      require:
        - '@git.9f6baefcc54c4e1791f7be073135c868e7adf496' #with Anton's OpenMP fix
    parallelio:
      require:
        - '@2.6.2'
    netcdf-c:
      require:
        - '@4.9.2'
        - build_system=cmake build_type=RelWithDebInfo
    netcdf-fortran:
      require:
        - '@4.6.1'
    openmpi:
      require:
        - '@4.1.7'
    gcc-runtime:
      require:
        - '%gcc'
    all:
      require:
        - '%oneapi@2025.0.4'
        - target=x86_64_v4
  view: true
  concretizer:
    unify: true
  modules:
    default:
      tcl:
        include:
          - access-om3
          - access3-exe
        projections:
          access-om3: '{name}/2025.x.0'
          access3-exe: '{name}/share_prototype-{hash:7}'
